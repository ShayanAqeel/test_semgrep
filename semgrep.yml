

rules:
  - id: print-debug-statements
    pattern: |
      hashlib.new(["md5", "sha1", "sha224", "sha256", "sha384", "sha512"])
    message: "Insecure hashing algorithm used."
    languages: [python]
    severity: ERROR
