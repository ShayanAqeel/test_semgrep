

rules:
  - id: print-debug-statements
    pattern: "['\"](api|key|secret)['\"]\s*:\s*['\"][^'\"]+['\"]"
    message: "Potential hardcoded secret or API key found."
    languages: [python]
    severity: HIGH
